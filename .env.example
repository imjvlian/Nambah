# Supabase — create a dedicated Nambah project before filling these values.
# Server-side database access used by catalog/pricing repositories.
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Reserved for Supabase Auth/client features in later milestones.
# Publishable keys are safe for browser use only when RLS policies are correct.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
